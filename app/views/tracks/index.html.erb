<% provide(:title, "top") %>

<div class="row">
  <%= render 'tracks/menus/index' %>
</div>

<div class="row row-eq-height tracks-container">
  <% if Track.count >= 1 %>
    <%= render @tracks %>
  <% end %>
</div>

<div class="row">
  <%= will_paginate @tracks %>
</div>
