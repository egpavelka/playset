def track_verification_middleware(url):
    # Verify that url is valid and from a supported service; set 'service'
    # Verify that link is to a single song (not playlist or  album)
    # Call corresponding service API or web scraper
    # Return metadata to pre-populate track final submission form
